
ABCD
[1,1,2,3]
int[] array = {A,B,C,D};
for(int i = 0; i<array.length; i++){
    for(int j = 0; j<array.length-i-1; j++){
        if(array[j] < array[j+1]){
            int aux = array[j];
            array[j] = array[j+1];
            array[j+1] = aux;
        }
    }
}

 float result1 = (float)Math.sqrt((float)Math.pow(array[0] - array[2],2) + (float)Math.pow(array[1] - array[3],2));
        float result2 = (float)Math.sqrt((float)Math.pow(array[0] - array[3],2) + (float)Math.pow(array[1] - array[2],2));
        
        if(result1 > result2){
            return (int)(result1);
        }else{
            return (int)(result2);
        }